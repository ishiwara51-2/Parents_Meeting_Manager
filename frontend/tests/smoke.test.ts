/**
 * smoke test — フロントエンド vitest が動作することを確認するだけのテスト。
 * Phase 4.1 完了条件：vitest が `npm test -- --run` で実行できること。
 */

describe('smoke test', () => {
  it('vitest が動作する', () => {
    expect(true).toBe(true)
  })
})
