/**
 * 保存完了画面
 * requirements.md §4.8
 * - PDF出力ボタン（Phase 5.2 で実装）
 * - 再編集ボタン
 * 実装は Phase 4.4c で行う
 */

export default function SavedPage() {
  return (
    <div>
      <h1>保存完了</h1>
      <p>Phase 4.4c で実装予定</p>
    </div>
  )
}
