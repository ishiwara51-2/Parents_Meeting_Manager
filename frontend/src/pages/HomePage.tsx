/**
 * ホーム画面
 * requirements.md §4.2
 * - 面談調整開始（新規プロジェクト作成）
 * - 過去プロジェクト一覧（作成日時降順）
 * - ルール設定
 * 実装は Phase 4.2 で行う
 */

export default function HomePage() {
  return (
    <div>
      <h1>ホーム</h1>
      <p>Phase 4.2 で実装予定</p>
    </div>
  )
}
